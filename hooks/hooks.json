{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/scripts/session-start.sh"
      }
    ],
    "PreCompact": [
      {
        "type": "prompt",
        "prompt": "Before compacting context, update the continuity ledger with current progress. Use dev_ledger(action='status') to check current state, then remind user to manually update checkboxes if needed."
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/detect-git-action.sh \"$TOOL_INPUT\" \"$TOOL_OUTPUT\""
          }
        ]
      }
    ]
  }
}
